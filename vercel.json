{
  "version": 2,
  "framework": null,
  "buildCommand": "echo \"No build step required\"",
  "installCommand": "npm install --workspaces --include-workspace-root",
  "outputDirectory": "apps/web/src",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
